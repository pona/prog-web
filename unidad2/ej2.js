// Programar un contador de caracteres para un elemento textarea

function contador() {
    alert(`Cantidad de caracteres en el textarea: ${document.getElementById('textarea1').textContent.length}`);   
}

contador();

/* Declarar variables de tipo string, number y boolean,
e imprimir una frase con interpolación de strings (``). */

const stringType = 'tipo string'
const numberType = 10
const booleanType = true

console.log(`Frase con interpolacion de stringType=${stringType}, 
    numberType=${numberType} y 
    booleanType=${booleanType}`)